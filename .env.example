# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# 중요: Service Role 키가 있어야 RLS를 우회하여 QR 스캔이 정상 작동합니다
# Supabase 대시보드 > Settings > API > Service role key에서 찾을 수 있습니다
# 이 키는 서버 사이드에서만 사용되므로 NEXT_PUBLIC_ 접두사를 붙이지 마세요
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Next.js Configuration
NEXTAUTH_SECRET=your-nextauth-secret
NEXTAUTH_URL=http://localhost:3000

# JWT Configuration
JWT_SECRET=your-jwt-secret-key-change-in-production

# Sentry Configuration (선택사항)
# Sentry 대시보드에서 프로젝트 생성 후 DSN을 입력하세요
# 개발 환경에서는 콘솔 로그만 출력되고 Sentry에 전송되지 않습니다
NEXT_PUBLIC_SENTRY_DSN=your-sentry-dsn-for-client
SENTRY_DSN=your-sentry-dsn-for-server

# Logging Configuration
# 로그 레벨: error, warn, info, debug
# 프로덕션: info, 개발: debug
LOG_LEVEL=debug